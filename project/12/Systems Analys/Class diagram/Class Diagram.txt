@startuml
' Настройка стиля
skinparam class {
    BackgroundColor #FEF9E7
    BorderColor #283747
    ArrowColor #283747
    FontName Arial
}

title Диаграмма классов - Сервис аренды автомобилей

' ===== Перечисления =====

enum РольПользователя {
    КЛИЕНТ
    АДМИНИСТРАТОР
    ВЛАДЕЛЕЦ
}

enum СтатусАвто {
    ДОСТУПЕН
    ЗАБРОНИРОВАН
    В_АРЕНДЕ
    НА_ОБСЛУЖИВАНИИ
    НЕДОСТУПЕН
}

enum СтатусБрони {
    ОЖИДАНИЕ
    ПОДТВЕРЖДЕНА
    АКТИВНА
    ЗАВЕРШЕНА
    ОТМЕНЕНА
}

enum СтатусПлатежа {
    ОЖИДАНИЕ
    ЗАБЛОКИРОВАНО
    ОПЛАЧЕНО
    ВОЗВРАЩЕНО
    ОШИБКА
}

' ===== Основные классы =====

class Пользователь {
    - id: Long
    - email: String
    - телефон: String
    - хешПароля: String
    - имя: String
    - фамилия: String
    - датаРегистрации: Date
    - роль: РольПользователя
    - верифицирован: Boolean
    + зарегистрироваться()
    + войти()
    + обновитьПрофиль()
    + верифицироватьДокументы()
}

class ВодительскиеПрава {
    - id: Long
    - номерПрав: String
    - датаВыдачи: Date
    - датаОкончания: Date
    - категории: String
    - фото_лицевая: String
    - фото_обратная: String
    - статусПроверки: Boolean
    + загрузить()
    + проверить()
    + проверитьСрок()
}

class СпособОплаты {
    - id: Long
    - токенКарты: String
    - последние4Цифры: String
    - владелецКарты: String
    - месяцОкончания: int
    - годОкончания: int
    - основной: Boolean
    + добавитьКарту()
    + удалитьКарту()
    + установитьОсновной()
}

class Автомобиль {
    - id: Long
    - марка: String
    - модель: String
    - годВыпуска: int
    - госНомер: String
    - цвет: String
    - коробкаПередач: String
    - типТоплива: String
    - количествоМест: int
    - ценаЗаСутки: BigDecimal
    - ценаЗаЧас: BigDecimal
    - суммаЗалога: BigDecimal
    - широта: Double
    - долгота: Double
    - статус: СтатусАвто
    - фотографии: List<String>
    + добавитьАвто()
    + обновитьСтатус()
    + обновитьМестоположение()
    + рассчитатьЦену()
}

class ХарактеристикиАвто {
    - id: Long
    - мощностьДвигателя: int
    - объемДвигателя: Double
    - расходТоплива: Double
    - объемБагажника: int
    - естьКондиционер: Boolean
    - естьПодогревСидений: Boolean
    - естьНавигатор: Boolean
    - естьBluetooth: Boolean
}

class Бронь {
    - id: Long
    - номерБрони: String
    - времяНачала: DateTime
    - времяОкончания: DateTime
    - фактВремяНачала: DateTime
    - фактВремяОкончания: DateTime
    - итоговаяЦена: BigDecimal
    - статус: СтатусБрони
    - причинаОтмены: String
    + создатьБронь()
    + подтвердитьБронь()
    + начатьАренду()
    + завершитьАренду()
    + отменитьБронь()
    + рассчитатьЦену()
}

class СессияАренды {
    - id: Long
    - пробегНачало: int
    - пробегКонец: int
    - уровеньТопливаНачало: Double
    - уровеньТопливаКонец: Double
    - времяНачала: DateTime
    - времяОкончания: DateTime
    - общеевремяМинут: int
    - общеерасстояние: int
    + начатьСессию()
    + завершитьСессию()
    + обновитьТелеметрию()
}

class Платеж {
    - id: Long
    - сумма: BigDecimal
    - заблокированнаяСумма: BigDecimal
    - итоговаяСумма: BigDecimal
    - статус: СтатусПлатежа
    - идентификаторТранзакции: String
    - датаПлатежа: DateTime
    - суммаВозврата: BigDecimal
    + заблокироватьПлатеж()
    + списатьПлатеж()
    + вернутьПлатеж()
    + получитьЧек()
}

class Отзыв {
    - id: Long
    - оценка: int (1-5)
    - комментарий: String
    - датаСоздания: DateTime
    + создатьОтзыв()
    + редактироватьОтзыв()
    + удалитьОтзыв()
}

class Чек {
    - id: Long
    - номерЧека: String
    - датаВыдачи: Date
    - позиции: List<ПозицияЧека>
    - суммаБезНалога: BigDecimal
    - налог: BigDecimal
    - итогоСумма: BigDecimal
    + сгенерировать()
    + отправитьНаПочту()
    + скачать()
}

class ПозицияЧека {
    - описание: String
    - количество: Double
    - ценаЗаЕдиницу: BigDecimal
    - сумма: BigDecimal
}

class Инцидент {
    - id: Long
    - тип: String (ДТП, ПОЛОМКА, ШТРАФ, ПРОСРОЧКА)
    - описание: String
    - фотографии: List<String>
    - датаСообщения: DateTime
    - датаРешения: DateTime
    - решение: String
    + сообщитьОбИнциденте()
    + решитьИнцидент()
    + рассчитатьШтраф()
}

class Штраф {
    - id: Long
    - сумма: BigDecimal
    - причина: String
    - датаНачисления: DateTime
    - оплачен: Boolean
    + начислитьШтраф()
    + отменитьШтраф()
}

class Уведомление {
    - id: Long
    - тип: String
    - заголовок: String
    - сообщение: String
    - датаОтправки: DateTime
    - прочитано: Boolean
    + отправить()
    + отметитьКакПрочитанное()
}

' ===== Связи между классами =====

Пользователь "1" -- "0..*" ВодительскиеПрава : имеет
Пользователь "1" -- "0..*" СпособОплаты : добавляет
Пользователь "1" -- "0..*" Бронь : создает
Пользователь "1" -- "0..*" Отзыв : пишет
Пользователь "1" -- "0..*" Уведомление : получает
Пользователь "1" -- "0..*" Инцидент : сообщает о

Автомобиль "1" -- "0..1" ХарактеристикиАвто : имеет
Автомобиль "1" -- "0..*" Бронь : участвует в
Автомобиль "1" -- "0..*" Отзыв : получает
Автомобиль "1" -- "0..*" Инцидент : связан с

Бронь "1" -- "1" СессияАренды : порождает
Бронь "1" -- "1..*" Платеж : включает
Бронь "1" -- "1" Чек : формирует
Бронь "1" -- "0..*" Штраф : может иметь

СессияАренды "1" -- "0..*" Инцидент : генерирует

Платеж "1" -- "0..*" Штраф : покрывает

Чек "1" -- "1..*" ПозицияЧека : содержит

' ===== Агрегация и композиция =====

' Агрегация: Автопарк содержит автомобили
Автомобиль "*" --* "1" Автопарк : входит в состав

class Автопарк {
    - id: Long
    - название: String
    - количествоМашин: int
    + добавитьАвто()
    + удалитьАвто()
    + получитьДоступныеАвто()
}

' Композиция: Транзакция не существует без платежа
Платеж "1" *-- "0..*" ЛогТранзакций : записывает

class ЛогТранзакций {
    - id: Long
    - действие: String
    - время: DateTime
    - детали: String
}

' ===== Агрегированные отчеты =====

class Отчет {
    - id: Long
    - тип: String
    - период: String
    - датаГенерации: DateTime
    - данные: String
    + сгенерироватьОтчет()
    + экспортировать()
}

Администратор "1" -- "0..*" Отчет : создает

class Администратор {
    - id: Long
    - праваДоступа: List<String>
    + управлятьПользователями()
    + управлятьАвто()
    + модерироватьОтзывы()
    + просматриватьОтчеты()
}

class ВладелецАвто {
    - id: Long
    - названиеКомпании: String
    - ИНН: String
    + добавитьАвтоВПарк()
    + просмотретьСтатистику()
    + вывестиДоход()
}

ВладелецАвто "1" -- "0..*" Автомобиль : владеет
ВладелецАвто "1" -- "1" Автопарк : управляет
@enduml