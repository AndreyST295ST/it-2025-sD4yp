@startuml
' Настройка стиля
skinparam activity {
    BackgroundColor #FEF9E7
    BorderColor #283747
    FontName Arial
    ArrowColor #283747
}

' Заголовок
title Деятельность пользователя (Клиента)
' Описание процесса аренды автомобиля с точки зрения пользователя

' Начало диаграммы
start

:Открывает мобильное приложение / сайт;

' Регистрация/вход
if (Уже есть аккаунт?) then (Нет)
    :Создает новый аккаунт\n(email/телефон);
else (Да)
    :Вводит логин и пароль;
endif

' Верификация
:Загружает фото документов;
:Вводит данные документа;
:Ожидает подтверждения верификации;

if (Документы отклонены?) then (Да)
    :Перезагружает фото /\nисправляет данные;
    :Повторно отправляет на проверку;
    note right: Цикл до успешной верификации
    kill ' Возврат к загрузке документов
else (Нет)
    :Получает статус "Верифицирован";
endif

' Привязка карты
:Переходит в раздел "Способы оплаты";
:Вводит данные банковской карты;
:Подтверждает привязку (3D-Secure);

if (Карта не принимается?) then (Да)
    :Пробует другую карту;
    note right: Повтор ввода данных карты
    kill
else (Нет)
    :Карта успешно привязана;
endif

' Поиск автомобиля
:Открывает карту или список авто;
:Применяет фильтры\n(цена, класс авто, тип КПП);
:Просматривает доступные автомобили;
:Выбирает понравившийся автомобиль;
:Открывает детальную карточку авто;

if (Характеристики устраивают?) then (Нет)
    :Возвращается к списку / карте;
    note right: Повторный выбор авто
    kill
else (Да)
    :Переходит к бронированию;
endif

' Бронирование
:Нажимает кнопку "Забронировать";
:Выбирает даты и время аренды;
:Выбирает тариф\n(часовой / суточный);
:Проверяет итоговую стоимость;

if (Стоимость устраивает?) then (Нет)
    :Изменяет параметры / Отменяет;
    note right: Возврат к выбору\nпараметров
    kill
else (Да)
    :Подтверждает бронирование;
endif

' Ожидание подтверждения
:Видит экран "Обработка платежа";
:Получает уведомление о статусе брони;

if (Бронь отклонена?) then (Да)
    if (Причина: ошибка оплаты?) then (Да)
        :Меняет карту;
    else (Нет - авто недоступно)
        :Возвращается к выбору авто;
    endif
    note right: Повторный цикл бронирования
    kill
else (Нет)
    :Переходит к экрану "Авто готов";
endif

' Получение авто
:Идет к месту стоянки автомобиля;
:Подписывает документы для аренды;
:Выдача автомобиля;


' Поездка
:Садится в автомобиль;
:Регулирует настройки под себя;
:Совершает поездку;

' Продление (опционально)
if (Нужно продлить аренду?) then (Да)
    :Нажимает "Продлить";
    if (Продление возможно?) then (Да)
        :Время аренды увеличено;
        :Продолжает поездку;
    else (Нет)
        :Готовится к завершению;
    endif
endif

' Завершение
:Паркует автомобиль в разрешенном месте;
:Проверяет, не оставил ли вещи;
:Сдает автомобиль;

' Оплата и чек
:Получает итоговую стоимость поездки;
:Проверяет детализацию\n(время, пробег, тариф);

if (Есть расхождения / вопросы?) then (Да)
    :Обращается в поддержку;
    :Ожидает ответа и решения;
else (Нет)
    :Подтверждает оплату;
endif

:Получает электронный чек\nна email / в приложение;

' Завершение
:Закрывает приложение;

stop
@enduml